version: '3'

services:
  opa:
    image: openpolicyagent/opa:latest
    ports:
      - "8181:8181"
    command: [
      "run",
      "--server",
      "--log-level", "debug",
      "--addr=0.0.0.0:8181"
    ]
